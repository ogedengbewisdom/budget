
import classes from "./Logo.module.css"

const Logo = (props) => {
    return (
            <h2 className={classes.log}><span className={classes.special}>go</span>Bud<span className={classes.special}>get</span></h2>
    )
}


export default Logo